function App() {
    return  <h1>Hello!</h1>;
}

// To make the function or App usable in index.js. Needs to be exported
export default App;
// Next, if we want to use the app component, use import inside index.js